export type PageTitleType = {
    mainTitle: string;
};

export type textSpecs = {
    base: number;
    increment: number;
    textType: string;
    text: string;
}

export type pageSpecs = {
    pageTitle: string;
    subtitle: string;
    hasImage: boolean;
}