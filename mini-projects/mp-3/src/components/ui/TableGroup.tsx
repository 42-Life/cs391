import styled from "styled-components";
import {GroupedContent} from "./GroupedContent.tsx";

export const TableGroup = styled(GroupedContent)`
     display: flex;
     flex-direction: column;
     float: left;
     width: 50%;
`;

